# Absolute origin of the deployed site, used for social/OG meta tags (og:image, og:url).
# LinkedIn and most scrapers require an absolute URL for the unfurl image to resolve.
# Leave unset in local dev (falls back to relative paths). Set at build/deploy time.
VITE_SITE_URL=https://hireloom.app
